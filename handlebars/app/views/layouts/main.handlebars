<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
	<!-- pass a variable at title, note 2 braces -->
    <title>{{title}}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/assets/css/style.css" />
</head>
<body>
	<!-- put up a dark nav bar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Node.js Fundamentals</a>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/guitars">Guitars</a>
                    </li>
                </ul>
                <div class="d-flex">
					<!-- navbar login/out links, is the user loged in? -->
                    {{#if user}}
					<!-- yes, display a logout button and link to the logout action -->
                    <a href="/logout" class="btn btn-outline-success">Logout</a>
                    {{else}}
					<!-- no, display a a login button and link to the login task -->
                    <a href="/login" class="btn btn-outline-success">Login</a>
                    {{/if}}
                </div>
            </div>
        </div>
        
    </nav>
    <div class="container content">
		<!-- pass the body code, note 3 braces -->
        {{{body}}}

    </div>
</body>
</html>